---
# tasks file for apt-packages
- name: Install `apt` packages
  become: true
  ansible.builtin.apt:
    pkg:
      - net-tools
      - nano
      - parted
      - cockpit
      - unzip
    state: present
    update_cache: false
